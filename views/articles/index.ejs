<!DOCTYPE html>
<html lang="en">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>BLOG POST APP</title>
    </head>

    <body style="box-sizing: border-box; background-color: #f9f7f4;" class="m-0 p-0 container-fluid ">
        <div class="container mt-5">
            <h1 class="mb-4">Blog Articles
            </h1>
            <a class="btn btn-outline-success" href="/articles/new-Article">Create New Article</a>
        </div>
        <div class="container d-flex align-content-center gap-3">
            <!-- This is to b in written in the EJS templating language -->
            <% articles.forEach(article=> { %>
                <div class="card mt-4 w-25 bg-warning p-2  bg-opacity-75">
                    <div id="dash-board-card" class="card-body">
                        <div class="card-title text-xl-start rounded-2">
                            <strong>
                                <%= article.title %>
                            </strong>
                        </div>
                        <div class="card-subtitle text-muted ">
                            <%= article.date.toLocaleDateString() %>
                        </div>
                        <div style="height:90px; padding-bottom:100px; overflow: hidden;" class="card-text mb-3">
                            <%= article.description %>
                        </div>
                        <a href="articles/<%= article.id %>" role="button" class="btn btn-outline-primary">Read More</a>
                        <form class="d-inline" method="POST" action="/articles/<%= article.id %>?_method=DELETE">
                            <button class="btn btn-outline-danger" type="submit">Delete</button>
                        </form>
                    </div>
                </div>
                <% }) %>
        </div>
    </body>

</html>